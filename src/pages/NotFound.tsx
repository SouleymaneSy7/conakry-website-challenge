import React from "react";
import ErrorsPage from "@/components/ErrorsPage";

const NotFound = () => {
  return (
    <React.Fragment>
      <ErrorsPage />
    </React.Fragment>
  );
};

export default NotFound;
