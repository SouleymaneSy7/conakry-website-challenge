import HomeHeader from "@/components/HomeHeader";
import React from "react";

const Home = () => {
  return (
    <React.Fragment>
      <HomeHeader />
    </React.Fragment>
  );
};

export default Home;
